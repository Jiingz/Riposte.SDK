namespace Riposte.SDK
{
    /// <summary>
    /// Contains all different plugin types which are set at the <see cref="IPlugin"/> Interface.
    /// </summary>
    public enum PluginType
    {
        Orbwalker,
        Prediction,
        Champion,
        Utility
    }
}